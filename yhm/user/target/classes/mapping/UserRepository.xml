<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yhm.repository.UserRepository">


    <select id="findAll" resultType="User">
        select * from t_user limit #{param1},#{param2}
    </select>

    <select id="count" resultType="int">
        select count(id) from t_user
    </select>

    <select id="findById" parameterType="long" resultType="User">
        select * from t_user where id = #{id}
    </select>

    <insert id="save" parameterType="User">
        insert into t_user(id,username,password,nickname,telephone) values(#{id},#{username},#{password},#{nickname},#{telephone})
    </insert>

    <update id="update" parameterType="User">
        update t_user set id=#{id},username=#{username},password=#{password},nickname=#{nickname},telephone=#{telephone} where id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        delete from t_user where id = #{id}
    </delete>

</mapper>